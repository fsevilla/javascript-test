function removeProperty(obj, property) {
    /*
        If a passed object has the specified property
        the function should delete the property from the object
        and return true.
        If it does not exist, the function should return false;
    */
    // Your code here
    return null;
}

function validatePerson(person) {
    /*
        The function should only accept a person with the following properties:
        - name
        - lastname
        - email
        - age
        If person is valid the function should return true.
        If the person is not valid, the function should clean the additional properties
        and return false
    */

    return null;
}   

function filterObjectsByProperty(haystack, propertyKey, propertyValue) {
    /*
        The function should filter all objects by a given property
        and return an array with the filtered objects.
    */

    return null;
}

function reverseWords(phrase) {
    /* 
        This function should return each word of a phrase reversed. 
        E.g. hello my world -> olleh ym dlrow
        Extra point if one liner.
    */

    return null;
}

function mostFrequentItem(haystack) {
    /*
        Write a function that returns a string with element that is found the most in an array
        E.g. [1,3,'a', 4, 'a', 3, 'a']
        Output: "a: 3 times"
        
        OPTIONAL for additional point:
        If 2 or more elements are the most found, it should print/return all of these in the following format:
        E.g. [1, 3,'a', 4, 'a', 3, 'a', 3]
        Output: "a and 3: 3 times"
    */

    return null;
}